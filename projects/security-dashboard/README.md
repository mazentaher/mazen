# Local Security Dashboard 🛡️

A tiny defensive dashboard that visualizes SSH authentication failures from a bundled sample log.

## Run

```bash
python3 app.py
```

Then open `http://127.0.0.1:8000` on the same machine.

The project intentionally uses only local sample data and binds to localhost. It is for authorized defensive learning only.
